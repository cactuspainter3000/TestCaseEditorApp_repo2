<UserControl x:Class="TestCaseEditorApp.MVVM.Controls.AnimatedChevron"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="16" d:DesignWidth="16">
    
    <TextBlock x:Name="ChevronText" 
               Text="{Binding ChevronSymbol, RelativeSource={RelativeSource AncestorType=UserControl}}"
               FontSize="{Binding ChevronSize, RelativeSource={RelativeSource AncestorType=UserControl}}" 
               Margin="{Binding ChevronMargin, RelativeSource={RelativeSource AncestorType=UserControl}}"
               Foreground="{Binding ChevronColor, RelativeSource={RelativeSource AncestorType=UserControl}}"
               RenderTransformOrigin="0.5,0.5"
               VerticalAlignment="Center"
               HorizontalAlignment="Center">
        
        <TextBlock.RenderTransform>
            <RotateTransform x:Name="ChevronRotation" Angle="0"/>
        </TextBlock.RenderTransform>
        
        <TextBlock.Style>
            <Style TargetType="TextBlock">
                <Style.Triggers>
                    <!-- Animated expansion trigger -->
                    <DataTrigger Binding="{Binding IsExpanded, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
                        <DataTrigger.EnterActions>
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetProperty="RenderTransform.Angle" 
                                                     To="180" 
                                                     Duration="0:0:0.2" />
                                </Storyboard>
                            </BeginStoryboard>
                        </DataTrigger.EnterActions>
                        <DataTrigger.ExitActions>
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetProperty="RenderTransform.Angle" 
                                                     To="0" 
                                                     Duration="0:0:0.2" />
                                </Storyboard>
                            </BeginStoryboard>
                        </DataTrigger.ExitActions>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </TextBlock.Style>
    </TextBlock>
</UserControl>