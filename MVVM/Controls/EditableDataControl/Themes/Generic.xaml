<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:EditableDataControl.Controls"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib">

    <!-- ===== Shared bits ===== -->
    <Style x:Key="IdBadgeBorder" TargetType="Border">
        <Setter Property="Background" Value="#3C3C3C"/>
        <Setter Property="CornerRadius" Value="10"/>
        <Setter Property="Padding" Value="2,0,2,0"/>
        <Setter Property="BorderBrush" Value="#555555"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
    </Style>

    <Style x:Key="IdBadgeText" TargetType="TextBlock">
        <Setter Property="FontSize" Value="10"/>
        <Setter Property="Foreground" Value="#AAAAAA"/>
        <Setter Property="Margin" Value="2,0,2,0"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <sys:Double x:Key="CellWidth">160</sys:Double>
    <sys:Double x:Key="RowHeaderWidth">36</sys:Double>

    <!-- ===== Debug/Dev context menus as RESOURCES (not types) ===== -->
    <ContextMenu x:Key="DebugCellMenu">
        <!-- Bind to VM commands living on the DataGrid's DataContext -->
        <MenuItem Header="Move Row Up"
                  Command="{Binding DataContext.MoveRowUpCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                  CommandParameter="{Binding}" />
        <MenuItem Header="Move Row Down"
                  Command="{Binding DataContext.MoveRowDownCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                  CommandParameter="{Binding}" />
    </ContextMenu>

    <ContextMenu x:Key="DebugColumnMenu">
        <!-- Add column header actions here as needed -->
        <MenuItem Header="(debug) Column Info"/>
    </ContextMenu>

    <!-- ===== Column header template (your existing editor) ===== -->
    <Style x:Key="EditorHeaderCard" TargetType="DataGridColumnHeader">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="VerticalContentAlignment" Value="Stretch"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGridColumnHeader">
                    <Border Background="{DynamicResource HeaderBackground}" BorderBrush="#555555" BorderThickness="0,0,1,0" Padding="0">
                        <Border BorderBrush="{DynamicResource AccentBrush}" BorderThickness="0,0,0,2" Padding="4">
                        <Grid DataContext="{Binding Column.Header, RelativeSource={RelativeSource AncestorType=DataGridColumnHeader}}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBox Grid.Column="0"
                                     Text="{Binding Header, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     Background="#2D2D30" Foreground="White"
                                     BorderThickness="0" HorizontalAlignment="Stretch" Margin="0,0,8,0"
                                     VerticalAlignment="Center"/>

                            <StackPanel Grid.Column="1" Orientation="Horizontal"
                                        HorizontalAlignment="Right" VerticalAlignment="Center">
                                <Button Content="◄"
                                        Command="{Binding DataContext.MoveColumnLeftCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}" FontSize="11" Margin="0,0,4,0"/>
                                <Button Content="►"
                                        Command="{Binding DataContext.MoveColumnRightCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}" FontSize="11" Margin="0,0,4,0"/>
                                <Button Content="Delete"
                                        Command="{Binding DataContext.DeleteColumnCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}" FontSize="11"/>
                            </StackPanel>
                        </Grid>
                        </Border>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===== Control template ===== -->
    <Style TargetType="{x:Type local:EditableDataControl}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:EditableDataControl}">
                    <Border x:Name="PART_Border"
                            Padding="12"
                            DataContext="{Binding EditorViewModel, RelativeSource={RelativeSource TemplatedParent}}">
                        <DockPanel>

                            <!-- Toolbar -->
                            <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" VerticalAlignment="Center" Margin="0,0,0,8">
                                <TextBlock Text="Title:" Foreground="White" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBox Width="320" Text="{Binding Title, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                         Background="{DynamicResource MenuBackground}" Foreground="White" 
                                         BorderBrush="{DynamicResource AccentBrush}" BorderThickness="0,0,0,2" Padding="8,4"/>
                                <Separator Margin="12,0,12,0" Background="#555555"/>
                                <Button Content="Add Column" Command="{Binding AddColumnCommand}" Margin="0,0,8,0"
                                        Padding="10,5">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Background" Value="{DynamicResource MenuHover}"/>
                                            <Setter Property="Foreground" Value="White"/>
                                            <Setter Property="BorderBrush" Value="#555555"/>
                                            <Setter Property="BorderThickness" Value="1"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}"
                                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                                CornerRadius="4"
                                                                Padding="{TemplateBinding Padding}">
                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="{DynamicResource MenuBackground}"/>
                                                    <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                                </Trigger>
                                                <Trigger Property="IsPressed" Value="True">
                                                    <Setter Property="Background" Value="{DynamicResource HeaderBackground}"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                                <Button Content="Add Row" Command="{Binding AddRowCommand}"
                                        Padding="10,5">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Background" Value="{DynamicResource MenuHover}"/>
                                            <Setter Property="Foreground" Value="White"/>
                                            <Setter Property="BorderBrush" Value="#555555"/>
                                            <Setter Property="BorderThickness" Value="1"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}"
                                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                                CornerRadius="4"
                                                                Padding="{TemplateBinding Padding}">
                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="{DynamicResource MenuBackground}"/>
                                                    <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                                </Trigger>
                                                <Trigger Property="IsPressed" Value="True">
                                                    <Setter Property="Background" Value="{DynamicResource HeaderBackground}"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </StackPanel>

                            <!-- Main grid -->
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <DataGrid x:Name="PART_DataGrid"
                                          Grid.Row="0"
                                          ItemsSource="{Binding Rows}"
                                          AutoGenerateColumns="False"
                                          CanUserAddRows="False"
                                          CanUserDeleteRows="False"
                                          CanUserReorderColumns="True"
                                          CanUserResizeColumns="True"
                                          CanUserSortColumns="False"
                                          HeadersVisibility="Column"
                                          GridLinesVisibility="All"
                                          RowHeaderWidth="{StaticResource RowHeaderWidth}"
                                          AlternationCount="2"
                                          HorizontalScrollBarVisibility="Auto"
                                          VerticalScrollBarVisibility="Auto"
                                          SelectionUnit="CellOrRowHeader"
                                          IsReadOnly="False"
                                          Background="{DynamicResource MenuHover}"
                                          Foreground="{DynamicResource MenuForeground}"
                                          RowBackground="{DynamicResource MenuHover}"
                                          AlternatingRowBackground="{DynamicResource MenuBackground}"
                                          BorderBrush="#555555"
                                          ColumnHeaderStyle="{StaticResource EditorHeaderCard}"
                                          local:DebugCellMenu.Enable="True"
                                          local:DebugColumnMenu.EnableHeader="True"
                                          local:EditorGridColumnBuilder.ColumnsSource="{Binding Columns}"
                                          local:EditorGridColumnBuilder.ColumnWidth="{StaticResource CellWidth}">

                                    <!-- Column header context menu style (applies to all headers) -->
                                    <DataGrid.Resources>
                                        <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource EditorHeaderCard}">
                                            <Setter Property="ContextMenu" Value="{StaticResource DebugColumnMenu}"/>
                                        </Style>
                                    </DataGrid.Resources>


                                    <DataGrid.RowHeaderTemplate>
                                        <DataTemplate>
                                            <Border Style="{StaticResource IdBadgeBorder}" HorizontalAlignment="Right">
                                                <TextBlock Style="{StaticResource IdBadgeText}"
                                                           Text="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow},
                                                                          Path=AlternationIndex}" />
                                            </Border>
                                        </DataTemplate>
                                    </DataGrid.RowHeaderTemplate>
                                </DataGrid>

                            </Grid>

                        </DockPanel>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
