<UserControl x:Class="TestCaseEditorApp.MVVM.Controls.HierarchicalMenuControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:models="clr-namespace:TestCaseEditorApp.MVVM.Models"
             xmlns:controls="clr-namespace:TestCaseEditorApp.MVVM.Controls"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="200">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/TestCaseEditorApp;component/Resources/DesignSystem.xaml"/>
                <ResourceDictionary Source="/TestCaseEditorApp;component/Resources/Base/Typography.xaml"/>
                <ResourceDictionary Source="/TestCaseEditorApp;component/Resources/Controls/Button.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            
            <BooleanToVisibilityConverter x:Key="BoolToVis"/>
            
            <!-- String to Visibility Converter for Badge display -->
            <Style x:Key="StringToVisibilityStyle" TargetType="Border">
                <Setter Property="Visibility" Value="Visible"/>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding Badge}" Value="">
                        <Setter Property="Visibility" Value="Collapsed"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Badge}" Value="{x:Null}">
                        <Setter Property="Visibility" Value="Collapsed"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
            
            <!-- Level 1 Template: Main sections using CollapsibleSection -->
            <DataTemplate x:Key="Level1Template" DataType="{x:Type models:MenuHierarchyItem}">
                <controls:CollapsibleSection HeaderText="{Binding Title}"
                                           IsExpanded="{Binding IsExpanded, Mode=TwoWay}"
                                           Margin="0,0,0,8">
                    <controls:CollapsibleSection.SectionContent>
                        <ItemsControl ItemsSource="{Binding Children}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <ContentPresenter Content="{Binding}" ContentTemplate="{DynamicResource Level2Template}"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </controls:CollapsibleSection.SectionContent>
                </controls:CollapsibleSection>
            </DataTemplate>
            
            <!-- Level 2 Template: Subsections -->
            <DataTemplate x:Key="Level2Template" DataType="{x:Type models:MenuHierarchyItem}">
                <Border Background="#f5f5f5" 
                        BorderBrush="#e0e0e0" 
                        BorderThickness="0,1,0,0" 
                        Margin="8,0,0,4"
                        Padding="8">
                    <StackPanel>
                        <!-- Subsection Header -->
                        <ToggleButton Content="{Binding Title}"
                                      IsChecked="{Binding IsExpanded, Mode=TwoWay}"
                                      HorizontalAlignment="Stretch"
                                      Margin="0,0,0,4"
                                      Visibility="{Binding IsExpandable, Converter={StaticResource BoolToVis}}"
                                      Background="LightGray" Foreground="Black" Padding="8,4">
                            <ToggleButton.ContentTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <controls:AnimatedChevron Grid.Column="0"
                                                                  IsExpanded="{Binding IsChecked, RelativeSource={RelativeSource AncestorType=ToggleButton}}"
                                                                  Margin="0,0,8,0"/>
                                        <TextBlock Grid.Column="1" Text="{Binding}" VerticalAlignment="Center"/>
                                        <Border Grid.Column="2" 
                                                Background="#007acc" 
                                                CornerRadius="8" 
                                                Padding="6,2" 
                                                Margin="8,0,0,0">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding DataContext.Badge, RelativeSource={RelativeSource AncestorType=ToggleButton}}" Value="">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding DataContext.Badge, RelativeSource={RelativeSource AncestorType=ToggleButton}}" Value="{x:Null}">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <TextBlock Text="{Binding DataContext.Badge, RelativeSource={RelativeSource AncestorType=ToggleButton}}"
                                                       Foreground="White" 
                                                       FontSize="10" 
                                                       FontWeight="Bold"/>
                                        </Border>
                                    </Grid>
                                </DataTemplate>
                            </ToggleButton.ContentTemplate>
                        </ToggleButton>
                        
                        <!-- Non-expandable item -->
                        <Button Content="{Binding Title}"
                                Command="{Binding Command}"
                                HorizontalAlignment="Stretch"
                                Margin="0,0,0,4"
                                Background="LightGray" Foreground="Black" Padding="8,4"
                                Visibility="{Binding IsExpandable, Converter={StaticResource InvertedBoolToVis}}"/>
                        
                        <!-- Level 2 Children -->
                        <ItemsControl ItemsSource="{Binding Children}"
                                      Visibility="{Binding IsExpanded, Converter={StaticResource BoolToVis}}"
                                      Margin="12,0,0,0">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <ContentPresenter Content="{Binding}" ContentTemplate="{DynamicResource Level4Template}"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </DataTemplate>
            
            <!-- Level 4 Template: Action items -->
            <DataTemplate x:Key="Level4Template" DataType="{x:Type models:MenuHierarchyItem}">
                <Button Content="{Binding Title}"
                        Command="{Binding Command}"
                        HorizontalAlignment="Stretch"
                        Margin="0,0,0,2"
                        Background="LightGray" Foreground="Black"
                        Padding="8,4">
                    <Button.ContentTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding DataContext.Icon, RelativeSource={RelativeSource AncestorType=Button}}"
                                           Margin="0,0,8,0"
                                           VerticalAlignment="Center"
                                           FontSize="12"/>
                                <TextBlock Text="{Binding}"
                                           VerticalAlignment="Center"/>
                            </StackPanel>
                        </DataTemplate>
                    </Button.ContentTemplate>
                </Button>
            </DataTemplate>
            
        </ResourceDictionary>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <ItemsControl x:Name="MenuItemsControl">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate DataType="{x:Type models:MenuHierarchyItem}">
                    <!-- Route to appropriate level template based on Level property -->
                    <ContentPresenter>
                        <ContentPresenter.Style>
                            <Style TargetType="ContentPresenter">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Level}" Value="1">
                                        <Setter Property="ContentTemplate" Value="{StaticResource Level1Template}"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Level}" Value="2">
                                        <Setter Property="ContentTemplate" Value="{StaticResource Level2Template}"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Level}" Value="4">
                                        <Setter Property="ContentTemplate" Value="{StaticResource Level4Template}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </ContentPresenter.Style>
                    </ContentPresenter>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </ScrollViewer>
</UserControl>