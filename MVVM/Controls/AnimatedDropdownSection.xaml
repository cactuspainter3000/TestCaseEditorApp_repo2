<UserControl x:Class="TestCaseEditorApp.MVVM.Controls.AnimatedDropdownSection"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:conv="clr-namespace:TestCaseEditorApp.Converters"
             xmlns:localControls="clr-namespace:TestCaseEditorApp.MVVM.Controls"
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="200">
    
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/TestCaseEditorApp;component/Resources/DesignSystem.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <conv:IsMainMenuItemToMarginConverter x:Key="IsMainMenuItemToMarginConverter" />
        </ResourceDictionary>
    </UserControl.Resources>

    <StackPanel>
        <!-- Header toggle button with conditional styling -->
        <ToggleButton x:Name="HeaderToggle"
                      Style="{StaticResource ActionToggleButton}"
                      HorizontalAlignment="Stretch"
                      HorizontalContentAlignment="Left"
                      Cursor="Hand"
                      Margin="{Binding IsMainMenuItem, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource IsMainMenuItemToMarginConverter}}"
                      IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}">
            <TextBlock Text="{Binding HeaderText, RelativeSource={RelativeSource AncestorType=UserControl}}">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Style.Triggers>
                            <!-- Level 1: Main menu items (IsMainMenuItem=True) - Orange -->
                            <DataTrigger Binding="{Binding IsMainMenuItem, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
                                <Setter Property="FontSize" Value="{StaticResource FontSize.H1}" />
                                <Setter Property="FontWeight" Value="SemiBold" />
                                <Setter Property="Foreground" Value="{StaticResource Brush.Accent.Primary}" />
                            </DataTrigger>
                            <!-- Level 2: Project, Requirement - White/Gray -->
                            <DataTrigger Binding="{Binding IsMainMenuItem, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="False">
                                <Setter Property="FontSize" Value="{StaticResource FontSize.H2}" />
                                <Setter Property="FontWeight" Value="Normal" />
                                <Setter Property="Foreground" Value="{StaticResource Brush.Text.Secondary}" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </ToggleButton>

        <!-- Animated expandable content -->
        <Border Margin="24,4,0,0" 
                Padding="0" 
                Background="Transparent" 
                ClipToBounds="True">
            <Border.RenderTransform>
                <ScaleTransform ScaleY="0" />
            </Border.RenderTransform>
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Opacity" Value="0" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsChecked, ElementName=HeaderToggle}" Value="True">
                            <DataTrigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" 
                                                         To="1" Duration="0:0:0.20" 
                                                         AccelerationRatio="0.15" DecelerationRatio="0.85"/>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity" 
                                                         To="1" Duration="0:0:0.14" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </DataTrigger.EnterActions>
                            <DataTrigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" 
                                                         To="0" Duration="0:0:0.16" 
                                                         AccelerationRatio="0.15" DecelerationRatio="0.85"/>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity" 
                                                         To="0" Duration="0:0:0.12" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </DataTrigger.ExitActions>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            
            <!-- Content placeholder -->
            <ContentPresenter Content="{Binding DropdownContent, RelativeSource={RelativeSource AncestorType=UserControl}}" />
        </Border>
    </StackPanel>
</UserControl>


