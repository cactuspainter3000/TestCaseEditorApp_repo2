<UserControl x:Class="TestCaseEditorApp.MVVM.Controls.CollapsibleSection"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:localControls="clr-namespace:TestCaseEditorApp.MVVM.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="250">
    
    <StackPanel>
        <!-- Header with integrated chevron -->
        <ToggleButton x:Name="SectionToggle"
                      Style="{StaticResource ActionToggleButton}"
                      HorizontalAlignment="Stretch"
                      Cursor="Hand"
                      Margin="{Binding HeaderMargin, RelativeSource={RelativeSource AncestorType=UserControl}}"
                      IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" 
                           Text="{Binding HeaderText, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                           FontSize="{Binding HeaderFontSize, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                           FontWeight="{Binding HeaderFontWeight, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                           HorizontalAlignment="Left" />
                <!-- Unified chevron -->
                <localControls:AnimatedChevron Grid.Column="1" 
                                               IsExpanded="{Binding IsChecked, ElementName=SectionToggle}"
                                               ChevronSize="{Binding ChevronSize, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                               ChevronMargin="{Binding ChevronMargin, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
            </Grid>
        </ToggleButton>

        <!-- Animated collapsible content area -->
        <Border Margin="{Binding ContentMargin, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                Padding="0" 
                Background="Transparent" 
                ClipToBounds="True">
            <Border.LayoutTransform>
                <ScaleTransform ScaleY="0" />
            </Border.LayoutTransform>
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Opacity" Value="0" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsChecked, ElementName=SectionToggle}" Value="True">
                            <DataTrigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="LayoutTransform.ScaleY" 
                                                         To="1" Duration="0:0:0.35" 
                                                         AccelerationRatio="0.15" DecelerationRatio="0.85"/>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity" 
                                                         To="1" Duration="0:0:0.25" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </DataTrigger.EnterActions>
                            <DataTrigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="LayoutTransform.ScaleY" 
                                                         To="0" Duration="0:0:0.30" 
                                                         AccelerationRatio="0.15" DecelerationRatio="0.85"/>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity" 
                                                         To="0" Duration="0:0:0.20" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </DataTrigger.ExitActions>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            
            <!-- Content placeholder -->
            <ContentPresenter Content="{Binding SectionContent, RelativeSource={RelativeSource AncestorType=UserControl}}" />
        </Border>
    </StackPanel>
</UserControl>