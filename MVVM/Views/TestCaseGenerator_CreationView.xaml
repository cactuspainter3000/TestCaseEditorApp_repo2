<UserControl x:Class="TestCaseEditorApp.MVVM.Views.TestCaseGenerator_CreationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800"
             Background="Transparent">
    
    <Grid Margin="20">
        <!-- Test Case Editor (full width) -->
        <Border
                Background="{StaticResource MenuBackground}"
                BorderBrush="{StaticResource CardBorderBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="16">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Placeholder when no test case selected -->
                    <TextBlock Grid.Row="0"
                               Grid.RowSpan="6"
                               Text="Select a test case from the list to edit"
                               FontSize="14"
                               Foreground="{StaticResource MenuForeground}"
                               Opacity="0.5"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedTestCase}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <!-- Editor content (visible when test case is selected) -->
                    <Grid Grid.Row="0" Grid.RowSpan="6">
                        <Grid.Style>
                            <Style TargetType="Grid">
                                <Setter Property="Visibility" Value="Visible"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedTestCase}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Grid.Style>
                        
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Title -->
                        <TextBlock Grid.Row="0"
                                   Text="Title"
                                   FontWeight="Bold"
                                   Foreground="{StaticResource MenuForeground}"
                                   Margin="0,0,0,4"/>
                        <TextBox Grid.Row="1"
                                 Text="{Binding SelectedTestCase.Title, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource MenuPopupTextBoxStyle}"
                                 Margin="0,0,0,16"/>

                        <!-- Preconditions -->
                        <TextBlock Grid.Row="2"
                                   Text="Preconditions"
                                   FontWeight="Bold"
                                   Foreground="{StaticResource MenuForeground}"
                                   Margin="0,0,0,4"/>
                        <TextBox Grid.Row="3"
                                 Text="{Binding SelectedTestCase.Preconditions, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource MenuPopupTextBoxStyle}"
                                 AcceptsReturn="True"
                                 TextWrapping="Wrap"
                                 MinHeight="60"
                                 VerticalScrollBarVisibility="Auto"
                                 Margin="0,0,0,16"/>

                        <!-- Steps -->
                        <TextBlock Grid.Row="4"
                                   Text="Test Steps"
                                   FontWeight="Bold"
                                   Foreground="{StaticResource MenuForeground}"
                                   Margin="0,0,0,4"/>
                        <TextBox Grid.Row="5"
                                 Text="{Binding SelectedTestCase.Steps, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource MenuPopupTextBoxStyle}"
                                 AcceptsReturn="True"
                                 TextWrapping="Wrap"
                                 MinHeight="120"
                                 VerticalScrollBarVisibility="Auto"
                                 Margin="0,0,0,16"/>

                        <!-- Expected Results -->
                        <TextBlock Grid.Row="6"
                                   Text="Expected Results"
                                   FontWeight="Bold"
                                   Foreground="{StaticResource MenuForeground}"
                                   Margin="0,0,0,4"/>
                        <TextBox Grid.Row="7"
                                 Text="{Binding SelectedTestCase.ExpectedResults, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource MenuPopupTextBoxStyle}"
                                 AcceptsReturn="True"
                                 TextWrapping="Wrap"
                                 MinHeight="80"
                                 VerticalScrollBarVisibility="Auto"
                                 Margin="0,0,0,16"/>
                    </Grid>
                </Grid>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>