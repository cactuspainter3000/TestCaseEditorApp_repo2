<UserControl x:Class="TestCaseEditorApp.MVVM.Views.TestCaseGenerator_CreationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="400">
    <Border Padding="10" Background="White" CornerRadius="6">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,8">
                <Button Content="Add Test" Command="{Binding AddTestCaseCommand}" Margin="4"/>
                <Button Content="Remove" Command="{Binding RemoveTestCaseCommand}" Margin="4"/>
            </StackPanel>

            <ListBox Grid.Row="1"
                     ItemsSource="{Binding TestCases}"
                     SelectedItem="{Binding SelectedTestCase, Mode=TwoWay}"
                     Margin="0,2,0,0"/>
        </Grid>
    </Border>
</UserControl>