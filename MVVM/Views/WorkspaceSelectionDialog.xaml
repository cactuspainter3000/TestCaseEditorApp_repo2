<Window x:Class="TestCaseEditorApp.MVVM.Views.WorkspaceSelectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AnythingLLM Workspace"
        Width="400" Height="200"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="#2B2B2B">
    
    <Border BorderBrush="#555555" BorderThickness="1" CornerRadius="8">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Custom Title Bar -->
            <Grid Grid.Row="0" Margin="0,0,0,10">
                <TextBlock x:Name="TitleTextBlock"
                           Text="AnythingLLM Workspace" 
                           Foreground="White" 
                           FontSize="16" 
                           FontWeight="SemiBold"
                           HorizontalAlignment="Left"
                           VerticalAlignment="Center"/>
                
                <Button Content="✕"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        Width="20" Height="20"
                        Background="Transparent"
                        BorderThickness="0"
                        Foreground="#CCCCCC"
                        FontSize="12"
                        Click="Cancel_Click"/>
            </Grid>

            <TextBlock x:Name="PromptTextBlock"
                       Grid.Row="1" 
                       Text="Enter workspace name:" 
                       Foreground="White" 
                       FontSize="14"
                       Margin="0,0,0,15"/>

            <TextBox x:Name="NewWorkspaceNameTextBox"
                     Grid.Row="2"
                     Width="300"
                     Height="30"
                     Style="{StaticResource MenuPopupTextBoxStyle}"
                     Margin="0,0,0,15"/>

            <!-- Workspace selection list (initially hidden) -->
            <ScrollViewer x:Name="WorkspaceScrollViewer"
                         Grid.Row="2"
                         Height="120"
                         Visibility="Collapsed"
                         Margin="0,0,0,15">
                <ListBox x:Name="WorkspacesListBox"
                         Background="#3C3C3C"
                         BorderBrush="#555555"
                         BorderThickness="1"
                         Foreground="White"
                         SelectionMode="Single">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="5">
                                <TextBlock Text="{Binding Name}" 
                                          FontWeight="Bold" 
                                          Foreground="#FF8C00"/>
                                <TextBlock Text="{Binding Slug}" 
                                          FontSize="11" 
                                          Foreground="#CCCCCC"/>
                                <TextBlock Text="{Binding CreatedAt, StringFormat='Created: {0:yyyy-MM-dd}'}" 
                                          FontSize="10" 
                                          Foreground="#999999"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </ScrollViewer>
            
            <StackPanel Grid.Row="3" 
                        Orientation="Horizontal" 
                        HorizontalAlignment="Right">
                
                <Button x:Name="CreateButton"
                        Content="Create"
                        Width="80" Height="32"
                        Background="#3C3C3C"
                        Foreground="#FF8C00"
                        BorderBrush="#555555"
                        BorderThickness="1"
                        FontSize="12"
                        FontWeight="Normal"
                        Cursor="Hand"
                        Margin="0,0,10,0"
                        Click="Create_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" 
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="3">
                                <ContentPresenter HorizontalAlignment="Center" 
                                                  VerticalAlignment="Center"
                                                  Margin="{TemplateBinding Padding}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#4A4A4A"/>
                                    <Setter Property="BorderBrush" Value="#FF8C00"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="#333333"/>
                                    <Setter Property="BorderBrush" Value="#FF8C00"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                
                <Button x:Name="CancelButton"
                        Content="Cancel"
                        Width="80" Height="32"
                        Background="#3C3C3C"
                        Foreground="#CCCCCC"
                        BorderBrush="#555555"
                        BorderThickness="1"
                        FontSize="12"
                        FontWeight="Normal"
                        Cursor="Hand"
                        Click="Cancel_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" 
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="3">
                                <ContentPresenter HorizontalAlignment="Center" 
                                                  VerticalAlignment="Center"
                                                  Margin="{TemplateBinding Padding}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#4A4A4A"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="#333333"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>