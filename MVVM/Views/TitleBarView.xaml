<UserControl x:Class="TestCaseEditorApp.MVVM.Views.TitleBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="40" d:DesignWidth="400">
    <DockPanel LastChildFill="False" VerticalAlignment="Center" Margin="4">
        <TextBlock Text="Systems App"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Left"
                   Foreground="DarkOrange"
                   FontSize="18"
                   Margin="6,0"/>
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,4,0">
            <Button Command="{Binding MinimizeCommand}" Style="{StaticResource MainWindowButtonControls}" Content="&#xE90F;" FontFamily="Segoe MDL2 Assets" FontSize="14"/>
            <Button Command="{Binding MaximizeRestoreCommand}" Style="{StaticResource MainWindowButtonControls}" Content="&#xE923;" FontFamily="Segoe MDL2 Assets" FontSize="14" Margin="4,0"/>
            <Button Command="{Binding CloseCommand}" Style="{StaticResource MainWindowCloseButton}" Content="✕" FontSize="14" Margin="4,0"/>
        </StackPanel>
    </DockPanel>
</UserControl>