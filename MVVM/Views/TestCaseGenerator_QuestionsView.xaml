<UserControl x:Class="TestCaseEditorApp.MVVM.Views.TestCaseGenerator_QuestionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:TestCaseEditorApp.MVVM.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="520" d:DesignWidth="800"
             Background="Transparent">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
                <!-- Questions list (visible when we have questions) -->
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <ScrollViewer.Style>
                        <Style TargetType="ScrollViewer">
                            <Setter Property="Visibility" Value="Visible"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding PendingQuestions.Count}" Value="0">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ScrollViewer.Style>
                    <ItemsControl ItemsSource="{Binding PendingQuestions}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="{x:Type local:ClarifyingQuestionVM}">
                                <Border Margin="0,0,0,12" 
                                        Background="#2D2D30" 
                                        BorderBrush="{StaticResource CardBorderBrush}" 
                                        BorderThickness="1" 
                                        CornerRadius="6" 
                                        Padding="16"
                                        x:Name="QuestionCard">
                                    <Grid x:Name="ContentGrid">
                                        <Grid.Style>
                                            <Style TargetType="Grid">
                                                <Setter Property="Opacity" Value="1"/>
                                                <Style.Triggers>
                                                    <!-- Fade out completely when marked for removal (assumption) -->
                                                    <DataTrigger Binding="{Binding IsFadingOut}" Value="True">
                                                        <DataTrigger.EnterActions>
                                                            <BeginStoryboard>
                                                                <Storyboard>
                                                                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                                     From="1.0"
                                                                                     To="0.0"
                                                                                     Duration="0:0:1.0">
                                                                        <DoubleAnimation.EasingFunction>
                                                                            <CubicEase EasingMode="EaseInOut"/>
                                                                        </DoubleAnimation.EasingFunction>
                                                                    </DoubleAnimation>
                                                                </Storyboard>
                                                            </BeginStoryboard>
                                                        </DataTrigger.EnterActions>
                                                    </DataTrigger>
                                                    <!-- Fade to 50% when submitted with answer (not an assumption) -->
                                                    <MultiDataTrigger>
                                                        <MultiDataTrigger.Conditions>
                                                            <Condition Binding="{Binding MarkedAsAssumption}" Value="False"/>
                                                            <Condition Binding="{Binding IsSubmitted}" Value="True"/>
                                                        </MultiDataTrigger.Conditions>
                                                        <MultiDataTrigger.EnterActions>
                                                            <BeginStoryboard>
                                                                <Storyboard>
                                                                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                                     To="0.5"
                                                                                     Duration="0:0:0.5">
                                                                        <DoubleAnimation.EasingFunction>
                                                                            <CubicEase EasingMode="EaseOut"/>
                                                                        </DoubleAnimation.EasingFunction>
                                                                    </DoubleAnimation>
                                                                </Storyboard>
                                                            </BeginStoryboard>
                                                        </MultiDataTrigger.EnterActions>
                                                    </MultiDataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Grid.Style>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>

                                        <!-- Header: Category and Severity badges -->
                                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8" Visibility="Collapsed">
                                            <Border Background="#444444" 
                                                    CornerRadius="3" 
                                                    Padding="6,2" 
                                                    Margin="0,0,6,0">
                                                <TextBlock Text="{Binding Category}" 
                                                           FontSize="10" 
                                                           FontWeight="SemiBold"
                                                           Foreground="#CCCCCC"/>
                                            </Border>
                                            <Border CornerRadius="3" 
                                                    Padding="6,2">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Background" Value="#555555"/>
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Severity}" Value="MUST">
                                                                <Setter Property="Background" Value="#8B4545"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Severity}" Value="SHOULD">
                                                                <Setter Property="Background" Value="#8B6F47"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Severity}" Value="OPTIONAL">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <TextBlock Text="{Binding Severity}" 
                                                           FontSize="10" 
                                                           FontWeight="SemiBold"
                                                           Foreground="#CCCCCC"/>
                                            </Border>
                                        </StackPanel>

                                        <!-- Question Text with Submitted Indicator -->
                                        <Grid Grid.Row="1" Margin="0,0,0,8">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            
                                            <TextBlock Grid.Column="0"
                                                       Text="{Binding Text}" 
                                                       TextWrapping="Wrap" 
                                                       FontSize="14"
                                                       FontWeight="SemiBold"
                                                       Foreground="{StaticResource MenuForeground}"/>
                                            
                                            <!-- Submitted Indicator -->
                                            <Border Grid.Column="1"
                                                    Background="#2E7D32"
                                                    CornerRadius="3"
                                                    Padding="6,2"
                                                    Margin="8,0,0,0"
                                                    VerticalAlignment="Top">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsSubmitted}" Value="True">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <TextBlock Text="✓ Submitted" 
                                                           FontSize="11"
                                                           FontWeight="Medium"
                                                           Foreground="White"/>
                                            </Border>
                                        </Grid>

                                        <!-- Rationale -->
                                        <TextBlock Grid.Row="2" 
                                                   Text="{Binding Rationale}" 
                                                   TextWrapping="Wrap" 
                                                   FontSize="12"
                                                   FontStyle="Italic" 
                                                   Foreground="{StaticResource MenuForeground}"
                                                   Opacity="0.7"
                                                   Margin="0,0,0,12"/>

                                        <!-- Answer Section and Actions -->
                                        <Grid Grid.Row="3">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <!-- Answer TextBox -->
                                            <Border Grid.Column="0" 
                                                    Background="Transparent" 
                                                    BorderBrush="{StaticResource CardBorderBrush}" 
                                                    BorderThickness="1" 
                                                    CornerRadius="4"
                                                    Margin="0,0,12,0">
                                                <Grid>
                                                    <TextBlock Text="Type your answer here or mark as assumption..." 
                                                               Foreground="#666666"
                                                               FontStyle="Italic"
                                                               Margin="8,8,8,8"
                                                               IsHitTestVisible="False">
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding Answer}" Value="">
                                                                        <Setter Property="Visibility" Value="Visible"/>
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding Answer}" Value="{x:Null}">
                                                                        <Setter Property="Visibility" Value="Visible"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>
                                                    <TextBox Text="{Binding Answer, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                                             AcceptsReturn="True" 
                                                             TextWrapping="Wrap"
                                                             MinHeight="60"
                                                             Background="Transparent"
                                                             BorderThickness="0"
                                                             Foreground="{StaticResource MenuForeground}"
                                                             CaretBrush="White"
                                                             Padding="8"
                                                             VerticalScrollBarVisibility="Auto"/>
                                                </Grid>
                                            </Border>

                                            <!-- Action Buttons -->
                                            <StackPanel Grid.Column="1" 
                                                        VerticalAlignment="Top">
                                                
                                                <!-- Mark as Assumption Checkbox -->
                                                <CheckBox IsChecked="{Binding MarkedAsAssumption, Mode=TwoWay}"
                                                          Foreground="{StaticResource MenuForeground}"
                                                          FontSize="12"
                                                          Margin="0,4,0,0">
                                                    <CheckBox.Content>
                                                        <TextBlock Text="Save as Global Assumption" 
                                                                   TextWrapping="Wrap"
                                                                   MaxWidth="140"/>
                                                    </CheckBox.Content>
                                                </CheckBox>
                                                
                                                <!-- Submit Button -->
                                                <Button Content="✓ Submit"
                                                        Command="{Binding DataContext.AcceptQuestionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding}" 
                                                        Background="#4A4A4A"
                                                        Foreground="{StaticResource MenuForeground}"
                                                        BorderThickness="1"
                                                        BorderBrush="{StaticResource CardBorderBrush}"
                                                        Padding="14,8"
                                                        Cursor="Hand"
                                                        FontWeight="Normal"
                                                        FontSize="12"
                                                        MinWidth="160">
                                                    <Button.Style>
                                                        <Style TargetType="Button">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                            <Setter Property="Template">
                                                                <Setter.Value>
                                                                    <ControlTemplate TargetType="Button">
                                                                        <Border Background="{TemplateBinding Background}" 
                                                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                                                CornerRadius="4" 
                                                                                Padding="{TemplateBinding Padding}">
                                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                        </Border>
                                                                    </ControlTemplate>
                                                                </Setter.Value>
                                                            </Setter>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsSubmitted}" Value="True">
                                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                                </DataTrigger>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter Property="Background" Value="#555555"/>
                                                                </Trigger>
                                                                <Trigger Property="IsPressed" Value="True">
                                                                    <Setter Property="Background" Value="#606060"/>
                                                                </Trigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Button.Style>
                                                </Button>
                                                
                                                <!-- Remove Button -->
                                                <Button Content="✕ Remove"
                                                        Command="{Binding DataContext.MarkResolvedCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding}"
                                                        Background="Transparent"
                                                        Foreground="{StaticResource MenuForeground}"
                                                        BorderBrush="{StaticResource CardBorderBrush}"
                                                        BorderThickness="1"
                                                        Padding="14,8"
                                                        Cursor="Hand"
                                                        FontSize="12"
                                                        MinWidth="160">
                                                    <Button.Style>
                                                        <Style TargetType="Button">
                                                            <Setter Property="Template">
                                                                <Setter.Value>
                                                                    <ControlTemplate TargetType="Button">
                                                                        <Border Background="{TemplateBinding Background}" 
                                                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                                                CornerRadius="4" 
                                                                                Padding="{TemplateBinding Padding}">
                                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                        </Border>
                                                                    </ControlTemplate>
                                                                </Setter.Value>
                                                            </Setter>
                                                            <Style.Triggers>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter Property="Background" Value="#1AFFFFFF"/>
                                                                </Trigger>
                                                                <Trigger Property="IsPressed" Value="True">
                                                                    <Setter Property="Background" Value="#33FFFFFF"/>
                                                                </Trigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Button.Style>
                                                </Button>
                                            </StackPanel>
                                        </Grid>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <!-- Instructional panel (visible when no questions) -->
                <Grid>
                    <Grid.Style>
                        <Style TargetType="Grid">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding PendingQuestions.Count}" Value="0">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="20" MaxWidth="750">
                        <TextBlock Text="🔍 Clarifying Questions (Optional)" 
                               FontSize="15" 
                               FontWeight="SemiBold" 
                               Foreground="{StaticResource Accent}" 
                               HorizontalAlignment="Center" 
                               Margin="0,0,0,16"/>

                    <Border Background="{StaticResource MenuBackground}" 
                            Padding="18" 
                            CornerRadius="8" 
                            BorderBrush="{StaticResource CardBorderBrush}" 
                            BorderThickness="1">
                        <StackPanel>
                            <TextBlock TextWrapping="Wrap" 
                                       Margin="0,0,0,12" 
                                       Foreground="{StaticResource MenuForeground}"
                                       FontSize="12"
                                       LineHeight="18">
                                <Run FontWeight="SemiBold">This step is optional.</Run>
                                <Run>You can skip directly to test case generation, or let the AI ask questions about things it finds ambiguous (and occasionally things that aren't).</Run>
                            </TextBlock>

                            <Border Background="#2D2D30" 
                                    Padding="14" 
                                    CornerRadius="6"
                                    Margin="0,0,0,0">
                                <StackPanel>
                                    <TextBlock Text="How it works:" 
                                               FontWeight="SemiBold" 
                                               Foreground="{StaticResource MenuForeground}"
                                               Margin="0,0,0,10"
                                               FontSize="11"/>

                                    <StackPanel Margin="0,0,0,6">
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                            <TextBlock Text="1" 
                                                       FontWeight="Bold" 
                                                       Foreground="{StaticResource MenuForeground}"
                                                       Width="18"
                                                       FontSize="11"/>
                                            <TextBlock TextWrapping="Wrap" 
                                                       Foreground="{StaticResource MenuForeground}"
                                                       FontSize="11">
                                                <Run>Click "</Run><Run FontWeight="SemiBold">Ask Clarifying Questions</Run><Run>" and the AI will find things to ask about</Run>
                                            </TextBlock>
                                        </StackPanel>
                                    </StackPanel>

                                    <StackPanel Margin="0,0,0,6">
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                            <TextBlock Text="2" 
                                                       FontWeight="Bold" 
                                                       Foreground="{StaticResource MenuForeground}"
                                                       Width="18"
                                                       FontSize="11"/>
                                            <TextBlock TextWrapping="Wrap" 
                                                       Foreground="{StaticResource MenuForeground}"
                                                       FontSize="11">
                                                <Run>Answer questions or mark as assumptions. </Run>
                                                <Run FontWeight="SemiBold">High-priority (MUST/SHOULD)</Run>
                                                <Run> answers trigger replacement questions for continuous flow.</Run>
                                            </TextBlock>
                                        </StackPanel>
                                    </StackPanel>

                                    <StackPanel Margin="0,0,0,6">
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                            <TextBlock Text="3" 
                                                       FontWeight="Bold" 
                                                       Foreground="{StaticResource MenuForeground}"
                                                       Width="18"
                                                       FontSize="11"/>
                                            <TextBlock TextWrapping="Wrap" 
                                                       Foreground="{StaticResource MenuForeground}"
                                                       FontSize="11">
                                                <Run>Checking </Run>
                                                <Run FontWeight="SemiBold">'Save as Global Assumption'</Run>
                                                <Run> teaches the AI to stop asking about this topic (results may vary)</Run>
                                            </TextBlock>
                                        </StackPanel>
                                    </StackPanel>

                                    <StackPanel>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="4" 
                                                       FontWeight="Bold" 
                                                       Foreground="{StaticResource MenuForeground}"
                                                       Width="18"
                                                       FontSize="11"/>
                                            <TextBlock TextWrapping="Wrap" 
                                                       Foreground="{StaticResource MenuForeground}"
                                                       FontSize="11">
                                                <Run>When only </Run>
                                                <Run FontWeight="SemiBold">OPTIONAL</Run>
                                                <Run> questions remain (or you're just done), the </Run>
                                                <Run FontWeight="SemiBold">'Generate Test Cases'</Run>
                                                <Run> button activates</Run>
                                            </TextBlock>
                                        </StackPanel>
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Border>
                </StackPanel>
                </Grid>
        </Grid>

        <!-- Footer: smart action button with progress indicator -->
        <Grid Grid.Row="1" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Loading indicator (shown when LLM is busy) -->
            <StackPanel Grid.Column="0" 
                        Orientation="Horizontal" 
                        HorizontalAlignment="Left" 
                        VerticalAlignment="Center"
                        x:Name="LoadingPanel"
                        Visibility="{Binding IsLlmBusy, Converter={StaticResource BoolToVisibilityConverter}}">
                <Viewbox Width="20" Height="20" Margin="0,0,10,0">
                    <Canvas Width="24" Height="24">
                        <Canvas.RenderTransform>
                            <RotateTransform x:Name="SpinnerRotate" CenterX="12" CenterY="12"/>
                        </Canvas.RenderTransform>
                        <Path Data="M12,4 A8,8 0 0,1 20,12" 
                              Stroke="{StaticResource Accent}" 
                              StrokeThickness="2"/>
                    </Canvas>
                </Viewbox>
                <TextBlock Text="{Binding StatusHint}" 
                           Foreground="{StaticResource MenuForeground}" 
                           FontSize="12"
                           FontStyle="Italic"
                           VerticalAlignment="Center"/>
                <StackPanel.Triggers>
                    <EventTrigger RoutedEvent="StackPanel.Loaded">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetName="SpinnerRotate"
                                                 Storyboard.TargetProperty="Angle"
                                                 From="0"
                                                 To="360"
                                                 Duration="0:0:1"
                                                 RepeatBehavior="Forever"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </StackPanel.Triggers>
            </StackPanel>

            <!-- Skip Questions Button -->
            <Button Grid.Column="1"
                    Content="Skip Questions" 
                    Command="{Binding SkipQuestionsCommand}"
                    Background="#3A3A3A"
                    Foreground="{StaticResource MenuForeground}"
                    BorderThickness="1"
                    BorderBrush="{StaticResource CardBorderBrush}"
                    Padding="16,10"
                    Margin="0,0,12,0"
                    FontWeight="Normal"
                    FontSize="13"
                    Cursor="Hand"
                    ToolTip="Skip clarifying questions and generate test cases with current assumptions only">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" 
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="4" 
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#454545"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#505050"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.3"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

            <!-- Action button -->
            <Button Grid.Column="2"
                    Content="{Binding SmartButtonLabel}" 
                    Command="{Binding SmartButtonCommand}" 
                    IsEnabled="{Binding CanExecuteSmartButton}"
                    Background="#4A4A4A"
                    Foreground="{StaticResource MenuForeground}"
                    BorderThickness="1"
                    BorderBrush="{StaticResource CardBorderBrush}"
                    Padding="18,10"
                    MinWidth="180"
                    FontWeight="Normal"
                    FontSize="13"
                    Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" 
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="4" 
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#555555"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#606060"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </Grid>
</UserControl>