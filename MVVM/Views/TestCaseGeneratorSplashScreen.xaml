<UserControl x:Class="TestCaseEditorApp.MVVM.Views.TestCaseGeneratorSplashScreen"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    
    <Grid>
        <Border Background="{StaticResource Brush.Background.Menu}" 
                BorderBrush="{StaticResource CardBorderBrush}" 
                BorderThickness="1" 
                CornerRadius="6" 
                Margin="20">
            
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="40">
                
                <!-- Icon - more subtle -->
                <TextBlock Text="📋" 
                           FontSize="32" 
                           HorizontalAlignment="Center" 
                           Opacity="0.6"
                           Margin="0,0,0,16"/>
                
                <!-- Title - smaller and less bold -->
                <TextBlock Text="Test Case Generator" 
                           FontSize="20" 
                           FontWeight="SemiBold" 
                           Foreground="{StaticResource MenuForeground}"
                           HorizontalAlignment="Center" 
                           Margin="0,0,0,8"/>
                
                <!-- Subtitle - more subtle -->
                <TextBlock Text="AI-Powered Test Case Generation" 
                           FontSize="13" 
                           Foreground="#999999"
                           HorizontalAlignment="Center" 
                           Margin="0,0,0,20"/>
                
                <!-- Description -->
                <TextBlock Text="{Binding Description}" 
                           FontSize="12" 
                           Foreground="#888888"
                           TextWrapping="Wrap"
                           TextAlignment="Center"
                           MaxWidth="360"
                           LineHeight="18"/>
                
            </StackPanel>
        </Border>
    </Grid>
</UserControl>