<Window x:Class="TestCaseEditorApp.MVVM.Views.UserSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Application Settings"
        Height="720"
        Width="860"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource Brush.Background.Menu}"
        Foreground="{StaticResource Brush.Text.Primary}">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/DesignSystem.xaml"/>
                <ResourceDictionary Source="/Resources/ButtonStyles.xaml"/>
                <ResourceDictionary Source="/Resources/ComboBoxStyles.xaml"/>
                <ResourceDictionary Source="/Resources/InputStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <!-- Shared style for labelled input rows -->
            <Style x:Key="FieldLabel" TargetType="TextBlock">
                <Setter Property="Foreground" Value="{StaticResource Brush.Text.Secondary}"/>
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="Margin" Value="0,0,0,4"/>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>  <!-- Window header -->
            <RowDefinition Height="*"/>     <!-- Tab content -->
            <RowDefinition Height="Auto"/>  <!-- Status -->
            <RowDefinition Height="Auto"/>  <!-- Actions -->
        </Grid.RowDefinitions>

        <!-- ── Window header ── -->
        <Border Grid.Row="0"
                Background="{StaticResource Brush.Background.Menu}"
                BorderBrush="{StaticResource Brush.Accent.Primary}"
                BorderThickness="0,0,0,1"
                Padding="20,14">
            <TextBlock Text="Configure user-level credentials and model preferences"
                       Foreground="White"
                       FontSize="15"
                       FontWeight="SemiBold"/>
        </Border>

        <!-- ── Tab control ── -->
        <TabControl Grid.Row="1"
                    Background="{StaticResource Brush.Background.Header}"
                    BorderBrush="{StaticResource Brush.Border.Default}"
                    BorderThickness="0"
                    SelectedIndex="{Binding SelectedSettingsTabIndex, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    Foreground="{StaticResource Brush.Text.Primary}">

            <TabControl.Template>
                <ControlTemplate TargetType="TabControl">
                    <Grid KeyboardNavigation.TabNavigation="Local" SnapsToDevicePixels="True">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TabPanel Grid.Row="0"
                                  IsItemsHost="True"
                                  Background="Transparent"
                                  Margin="10,0,10,0"/>

                        <Border Grid.Row="1"
                                BorderBrush="#666666"
                                BorderThickness="0,2,0,0"
                                Margin="10,0,10,0"
                                Background="Transparent">
                            <ContentPresenter x:Name="PART_SelectedContentHost"
                                              ContentSource="SelectedContent"/>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </TabControl.Template>

            <TabControl.Resources>
                <Style TargetType="{x:Type TabItem}">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="#CCCCCC"/>
                    <Setter Property="BorderBrush" Value="#555555"/>
                    <Setter Property="BorderThickness" Value="1,1,1,0"/>
                    <Setter Property="Padding" Value="15,8"/>
                    <Setter Property="Margin" Value="1,0"/>
                    <Setter Property="FontWeight" Value="Normal"/>
                    <Setter Property="FontSize" Value="13"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border x:Name="Root"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="4,4,0,0"
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter x:Name="ContentSite"
                                                      ContentSource="Header"
                                                      HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Root" Property="BorderBrush" Value="#777777"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Root" Property="BorderBrush" Value="#FF8C00"/>
                                        <Setter TargetName="Root" Property="Background" Value="#1E1E1E"/>
                                        <Setter Property="Foreground" Value="#FF8C00"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>

            <!-- ── Jama tab ── -->
            <TabItem Header="Jama" Foreground="{StaticResource Brush.Text.Primary}">
                <StackPanel Margin="20,20,20,0">

                    <!-- Base URL -->
                    <TextBlock Text="Base URL" Style="{StaticResource FieldLabel}"/>
                    <TextBox Text="{Binding JamaBaseUrl, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource MenuPopupTextBoxStyle}"
                             Margin="0,0,0,14"
                             FontSize="13"/>

                    <!-- Client ID -->
                    <TextBlock Text="Client ID" Style="{StaticResource FieldLabel}"/>
                    <TextBox Text="{Binding JamaClientId, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource MenuPopupTextBoxStyle}"
                             Margin="0,0,0,14"
                             FontSize="13"/>

                    <!-- Client Secret -->
                    <TextBlock Text="Client Secret" Style="{StaticResource FieldLabel}"/>
                    <TextBox Text="{Binding JamaClientSecret, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource MenuPopupTextBoxStyle}"
                             Margin="0,0,0,20"
                             FontSize="13"/>

                    <Button Content="Test Jama Connection"
                            HorizontalAlignment="Left"
                            Padding="16,8"
                            Command="{Binding TestJamaConnectionCommand}"
                            Style="{StaticResource ActionButton}"/>
                </StackPanel>
            </TabItem>

            <!-- ── AnythingLLM tab ── -->
            <TabItem Header="AnythingLLM" Foreground="{StaticResource Brush.Text.Primary}">
                <StackPanel Margin="20,20,20,0">

                    <!-- Base URL -->
                    <TextBlock Text="Base URL" Style="{StaticResource FieldLabel}"/>
                    <TextBox Text="{Binding AnythingLlmBaseUrl, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource MenuPopupTextBoxStyle}"
                             Margin="0,0,0,14"
                             FontSize="13"/>

                    <!-- API Key -->
                    <TextBlock Text="API Key" Style="{StaticResource FieldLabel}"/>
                    <TextBox Text="{Binding AnythingLlmApiKey, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource MenuPopupTextBoxStyle}"
                             Margin="0,0,0,20"
                             FontSize="13"/>

                    <Button Content="Test AnythingLLM Connection"
                            HorizontalAlignment="Left"
                            Padding="16,8"
                            Command="{Binding TestAnythingLlmConnectionCommand}"
                            Style="{StaticResource ActionButton}"/>
                </StackPanel>
            </TabItem>

            <!-- ── Ollama Models tab ── -->
            <TabItem Header="Ollama Models" Foreground="{StaticResource Brush.Text.Primary}">
                <StackPanel Margin="20,20,20,0">

                    <!-- Info banner -->
                    <Border Background="#3C3C3C"
                            BorderBrush="{StaticResource Brush.Border.Subtle}"
                            BorderThickness="0,0,0,1"
                            Padding="12,10"
                            Margin="0,0,0,18">
                        <TextBlock TextWrapping="Wrap"
                                   Foreground="#CCCCCC"
                                   FontSize="12">
                            <Run Text="Select the Ollama models installed on this machine. Click"/>
                            <Run Text=" Refresh " FontWeight="SemiBold" Foreground="{StaticResource Brush.Accent.Primary}"/>
                            <Run Text="to load the current list from Ollama."/>
                        </TextBlock>
                    </Border>

                    <!-- Chat Model -->
                    <TextBlock Text="Chat Model" Style="{StaticResource FieldLabel}"/>
                    <ComboBox ItemsSource="{Binding OllamaModels}"
                              SelectedItem="{Binding SelectedChatModel, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                              IsEditable="False"
                              Style="{StaticResource CardComboBoxStyle}"
                              Margin="0,0,0,14"
                              FontSize="13"/>

                    <!-- Embedding Model -->
                    <TextBlock Text="Embedding Model" Style="{StaticResource FieldLabel}"/>
                    <ComboBox ItemsSource="{Binding OllamaModels}"
                              SelectedItem="{Binding SelectedEmbeddingModel, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                              IsEditable="False"
                              Style="{StaticResource CardComboBoxStyle}"
                              Margin="0,0,0,20"
                              FontSize="13"/>

                    <Button Content="Refresh available models"
                            HorizontalAlignment="Left"
                            Padding="16,8"
                            Command="{Binding RefreshOllamaModelsCommand}"
                            Style="{StaticResource ActionButton}"/>

                        <Button Content="Test selected models"
                            HorizontalAlignment="Left"
                            Padding="16,8"
                            Margin="0,10,0,0"
                            Command="{Binding TestSelectedOllamaModelsCommand}"
                            Style="{StaticResource ActionButton}"/>

                        <TextBlock Text="Debug diagnostics"
                               Style="{StaticResource FieldLabel}"
                               Margin="0,16,0,6"/>

                        <TextBox Text="{Binding OllamaDebugInfo}"
                             IsReadOnly="True"
                             AcceptsReturn="True"
                             TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto"
                             Height="170"
                             Style="{StaticResource MenuPopupTextBoxStyle}"
                             FontSize="12"/>
                </StackPanel>
            </TabItem>

            <!-- ── Diagnostics tab ── -->
            <TabItem Header="Diagnostics" Foreground="{StaticResource Brush.Text.Primary}">
                <StackPanel Margin="20,20,20,0">

                    <Border Background="{StaticResource Brush.Background.Menu}"
                            BorderBrush="{StaticResource Brush.Accent.Primary}"
                            BorderThickness="0,0,0,1"
                            Padding="0,0,0,10"
                            Margin="0,0,0,16">
                        <TextBlock Text="Requirements Analysis Diagnostics"
                                   FontWeight="SemiBold"
                                   FontSize="13"
                                   Foreground="{StaticResource Brush.Accent.Primary}"/>
                    </Border>

                    <CheckBox Content="Enable Requirements Analysis Snapshot Logging"
                              IsChecked="{Binding EnableRequirementsAnalysisSnapshot, UpdateSourceTrigger=PropertyChanged}"
                              Foreground="{StaticResource Brush.Text.Primary}"
                              FontSize="13"
                              Margin="0,0,0,12"/>

                    <TextBlock Text="Setting changes are persisted when you click Save."
                               Foreground="{StaticResource Brush.Text.Secondary}"
                               FontSize="12"/>
                </StackPanel>
            </TabItem>
        </TabControl>

        <!-- ── Status message ── -->
        <Border Grid.Row="2"
                Background="#3A3A3A"
                Padding="20,10"
                Margin="0,0,0,0">
            <TextBlock Text="{Binding StatusMessage}"
                       TextWrapping="Wrap"
                       MinHeight="18">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Foreground" Value="{StaticResource Brush.Text.Secondary}"/>
                        <Setter Property="FontSize" Value="12"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsStatusError}" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource Brush.State.Error}"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </Border>

        <!-- ── Action buttons ── -->
        <Border Grid.Row="3"
                Background="{StaticResource Brush.Background.Menu}"
                BorderBrush="{StaticResource Brush.Border.Default}"
                BorderThickness="0,1,0,0"
                Padding="20,12">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Padding="20,8"
                        Margin="0,0,10,0"
                        Command="{Binding SaveAsyncCommand}">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
                            <Setter Property="Content" Value="Save"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HasUnsavedChanges}" Value="True">
                                    <Setter Property="Content" Value="Save *"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                <Button Content="Close"
                        Padding="20,8"
                        Command="{Binding CancelCommand}"
                        Style="{StaticResource ActionButton.Secondary}"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
