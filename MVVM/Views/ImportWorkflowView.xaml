<UserControl x:Class="TestCaseEditorApp.MVVM.Views.ImportWorkflowView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:TestCaseEditorApp.MVVM.ViewModels"
             d:DataContext="{d:DesignInstance vm:ImportWorkflowViewModel, IsDesignTimeCreatable=True}"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="600">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../../Resources/DesignSystem.xaml"/>
                <ResourceDictionary Source="../../Resources/ButtonStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            
            <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Import Requirements Document" 
                       FontSize="20" 
                       FontWeight="SemiBold" 
                       Foreground="{StaticResource Brush.Accent.Primary}"
                       Margin="0,0,0,8"/>
            <TextBlock Text="Configure your import settings and choose where to save your new project workspace."
                       FontSize="14"
                       Foreground="{StaticResource Brush.Text.Secondary}"
                       TextWrapping="Wrap"/>
        </StackPanel>

        <!-- Import Configuration Steps -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                
                <!-- Step 1: Select Document -->
                <Border Background="{StaticResource Brush.Background.Menu}" 
                        BorderBrush="{StaticResource Brush.Border.Default}" 
                        BorderThickness="1" 
                        CornerRadius="8" 
                        Padding="16"
                        Margin="0,0,0,20">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                            <TextBlock Text="1" 
                                       Background="{StaticResource Brush.Accent.Primary}" 
                                       Foreground="White" 
                                       Width="24" Height="24" 
                                       TextAlignment="Center" 
                                       VerticalAlignment="Center"
                                       FontWeight="Bold"
                                       FontSize="14"/>
                            <TextBlock Text="Select Requirements Document" 
                                       FontSize="16" 
                                       FontWeight="SemiBold" 
                                       Foreground="{StaticResource Brush.Text.Primary}"
                                       Margin="12,0,0,0"
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                        
                        <TextBlock Text="Choose the Word document containing your requirements (exported from Jama)"
                                   FontSize="12"
                                   Foreground="{StaticResource Brush.Text.Tertiary}"
                                   Margin="0,0,0,12"/>
                        
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <TextBox Grid.Column="0" 
                                     Text="{Binding SelectedDocumentPath}" 
                                     IsReadOnly="True"
                                     Background="{StaticResource Brush.Background.Menu.Hover}"
                                     Margin="0,0,8,0"
                                     Padding="8"
                                     VerticalContentAlignment="Center"/>
                            
                            <Button Grid.Column="1" 
                                    Content="Browse..." 
                                    Command="{Binding SelectDocumentCommand}"
                                    Style="{StaticResource MenuPopupButtonStyle}"
                                    Padding="12,8"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Step 2: Import Options -->
                <Border Background="{StaticResource Brush.Background.Menu}" 
                        BorderBrush="{StaticResource Brush.Border.Default}" 
                        BorderThickness="1" 
                        CornerRadius="8" 
                        Padding="16"
                        Margin="0,0,0,20">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                            <TextBlock Text="2" 
                                       Background="{StaticResource Brush.Accent.Primary}" 
                                       Foreground="White" 
                                       Width="24" Height="24" 
                                       TextAlignment="Center" 
                                       VerticalAlignment="Center"
                                       FontWeight="Bold"
                                       FontSize="14"/>
                            <TextBlock Text="Configure Import Options" 
                                       FontSize="16" 
                                       FontWeight="SemiBold" 
                                       Foreground="{StaticResource Brush.Text.Primary}"
                                       Margin="12,0,0,0"
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                        
                        <!-- Auto Analyze Option -->
                        <CheckBox IsChecked="{Binding AutoAnalyzeRequirements}" Margin="0,0,0,12">
                            <StackPanel>
                                <TextBlock Text="Auto-analyze requirements during import" 
                                           FontWeight="SemiBold" 
                                           Foreground="{StaticResource Brush.Text.Primary}"/>
                                <TextBlock Text="Recommended: Automatically analyze each requirement for better test case generation" 
                                           FontSize="11" 
                                           Foreground="{StaticResource Brush.Text.Tertiary}" 
                                           Margin="0,2,0,0"/>
                            </StackPanel>
                        </CheckBox>
                        
                        <!-- Export for ChatGPT Option -->
                        <CheckBox IsChecked="{Binding ExportForChatGpt}">
                            <StackPanel>
                                <TextBlock Text="Export for ChatGPT analysis" 
                                           FontWeight="SemiBold" 
                                           Foreground="{StaticResource Brush.Text.Primary}"/>
                                <TextBlock Text="Export requirements to a file that can be used with ChatGPT for additional analysis" 
                                           FontSize="11" 
                                           Foreground="{StaticResource Brush.Text.Tertiary}" 
                                           Margin="0,2,0,0"/>
                            </StackPanel>
                        </CheckBox>
                    </StackPanel>
                </Border>

                <!-- Step 3: Workspace Location -->
                <Border Background="{StaticResource Brush.Background.Menu}" 
                        BorderBrush="{StaticResource Brush.Border.Default}" 
                        BorderThickness="1" 
                        CornerRadius="8" 
                        Padding="16">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                            <TextBlock Text="3" 
                                       Background="{StaticResource Brush.Accent.Primary}" 
                                       Foreground="White" 
                                       Width="24" Height="24" 
                                       TextAlignment="Center" 
                                       VerticalAlignment="Center"
                                       FontWeight="Bold"
                                       FontSize="14"/>
                            <TextBlock Text="Choose Project Workspace Location" 
                                       FontSize="16" 
                                       FontWeight="SemiBold" 
                                       Foreground="{StaticResource Brush.Text.Primary}"
                                       Margin="12,0,0,0"
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                        
                        <TextBlock Text="Choose where to save your new project workspace file"
                                   FontSize="12"
                                   Foreground="{StaticResource Brush.Text.Tertiary}"
                                   Margin="0,0,0,12"/>
                        
                        <TextBox Text="{Binding WorkspaceName}" 
                                 Margin="0,0,0,8"
                                 Padding="8"
                                 VerticalContentAlignment="Center"/>
                        
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <TextBox Grid.Column="0" 
                                     Text="{Binding WorkspaceSavePath}" 
                                     IsReadOnly="True"
                                     Background="{StaticResource Brush.Background.Menu.Hover}"
                                     Margin="0,0,8,0"
                                     Padding="8"
                                     VerticalContentAlignment="Center"/>
                            
                            <Button Grid.Column="1" 
                                    Content="Choose Location..." 
                                    Command="{Binding ChooseWorkspaceLocationCommand}"
                                    Style="{StaticResource MenuPopupButtonStyle}"
                                    Padding="12,8"/>
                        </Grid>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>

        <!-- Action Buttons -->
        <Grid Grid.Row="3" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <Button Grid.Column="1" 
                    Content="Cancel" 
                    Command="{Binding CancelCommand}"
                    Style="{StaticResource MenuPopupButtonStyle}"
                    Margin="0,0,8,0"
                    Padding="16,8"
                    MinWidth="80"/>
            
            <Button Grid.Column="2" 
                    Content="Start Import" 
                    Command="{Binding StartImportCommand}"
                    Style="{StaticResource PrimaryButton}"
                    Padding="16,8"
                    MinWidth="120"
                    IsEnabled="{Binding CanProceed}"/>
        </Grid>
    </Grid>
</UserControl>