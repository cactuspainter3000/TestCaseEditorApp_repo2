<UserControl x:Class="TestCaseEditorApp.MVVM.Views.RequirementsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:conv="clr-namespace:TestCaseEditorApp.Converters"
             xmlns:local="clr-namespace:TestCaseEditorApp.MVVM.Views"
             mc:Ignorable="d"
             MinHeight="400" MinWidth="800"
             HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
             d:DesignHeight="600" d:DesignWidth="900"
             Background="{StaticResource Brush.Background.Menu}"
             Loaded="UserControl_Loaded">

    <UserControl.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </UserControl.Resources>

    <!-- Simply display the TestCaseGenerator_VM through the DataTemplate system -->
    <ContentControl Content="{Binding TestCaseGeneratorVM}" />

</UserControl>