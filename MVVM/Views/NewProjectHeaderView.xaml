<UserControl x:Class="TestCaseEditorApp.MVVM.Views.NewProjectHeaderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="75" d:DesignWidth="800">
    
    <Border Background="#2B2B2B" Padding="12">
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
            <TextBlock Text="🚀" FontSize="24" Margin="0,0,12,0" VerticalAlignment="Center"/>
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="{Binding Title}" Foreground="Orange" FontWeight="Bold" FontSize="16"/>
                <TextBlock Text="{Binding Subtitle}" Foreground="#CCCCCC" FontSize="12"/>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>