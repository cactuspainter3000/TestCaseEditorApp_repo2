<UserControl x:Class="TestCaseEditorApp.MVVM.Views.TestFlowHeaderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:TestCaseEditorApp.MVVM.ViewModels">
    <Grid Margin="6">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="8"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Button Grid.Column="0" Style="{StaticResource MenuPopupButtonStyle}"
                Command="{Binding NewFlowCommand}">
            <TextBlock Text="New Flow" />
        </Button>

        <Button Grid.Column="2" Style="{StaticResource MenuPopupButtonStyle}"
                Command="{Binding ImportFlowCommand}">
            <TextBlock Text="Import Flow" />
        </Button>

        <TextBlock Grid.Column="3" Text="Test Flow Generator" VerticalAlignment="Center" HorizontalAlignment="Right"
                   Foreground="{StaticResource MenuForeground}" Opacity="0.7" />
    </Grid>
</UserControl>