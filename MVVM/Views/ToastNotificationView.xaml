<UserControl x:Class="TestCaseEditorApp.MVVM.Views.ToastNotificationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:models="clr-namespace:TestCaseEditorApp.MVVM.Models"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=models:ToastNotification}"
             MaxWidth="400"
             Margin="0,0,0,8">
    
    <UserControl.Resources>
        <Storyboard x:Key="SlideInStoryboard">
            <DoubleAnimation Storyboard.TargetName="ToastBorder"
                           Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                           From="450" To="0"
                           Duration="0:0:0.4"
                           DecelerationRatio="0.7"/>
            <DoubleAnimation Storyboard.TargetName="ToastBorder"
                           Storyboard.TargetProperty="Opacity"
                           From="0" To="1"
                           Duration="0:0:0.3"/>
        </Storyboard>
        
        <Storyboard x:Key="SlideOutStoryboard" Completed="SlideOut_Completed">
            <DoubleAnimation Storyboard.TargetName="ToastBorder"
                           Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                           From="0" To="450"
                           Duration="0:0:0.3"
                           AccelerationRatio="0.7"/>
            <DoubleAnimation Storyboard.TargetName="ToastBorder"
                           Storyboard.TargetProperty="Opacity"
                           From="1" To="0"
                           Duration="0:0:0.5"/>
        </Storyboard>
    </UserControl.Resources>
    
    <Border x:Name="ToastBorder"
            Background="#DD1E1E1E"
            BorderThickness="1"
            CornerRadius="6"
            Padding="16,12"
            RenderTransformOrigin="0.5,0.5">
        <Border.RenderTransform>
            <TranslateTransform/>
        </Border.RenderTransform>
        
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="BorderBrush" Value="#44888888"/>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding Type}" Value="Info">
                        <Setter Property="BorderBrush" Value="#5599CCFF"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Type}" Value="Success">
                        <Setter Property="BorderBrush" Value="#5500CC66"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Type}" Value="Warning">
                        <Setter Property="BorderBrush" Value="#66FFA500"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Type}" Value="Error">
                        <Setter Property="BorderBrush" Value="#66FF4444"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Border.Style>
        
        <Border.Effect>
            <DropShadowEffect Color="Black" 
                            Direction="270" 
                            ShadowDepth="4" 
                            BlurRadius="12" 
                            Opacity="0.5"/>
        </Border.Effect>
        
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <!-- Icon -->
            <TextBlock Grid.Column="0" 
                       FontSize="16" 
                       VerticalAlignment="Top"
                       Margin="0,0,12,0">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Text" Value="ℹ️"/>
                        <Setter Property="Foreground" Value="#99CCFF"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Type}" Value="Success">
                                <Setter Property="Text" Value="✓"/>
                                <Setter Property="Foreground" Value="#00CC66"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Type}" Value="Warning">
                                <Setter Property="Text" Value="⚠"/>
                                <Setter Property="Foreground" Value="#FFA500"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Type}" Value="Error">
                                <Setter Property="Text" Value="✗"/>
                                <Setter Property="Foreground" Value="#FF4444"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
            
            <!-- Message -->
            <TextBlock Grid.Column="1"
                       Text="{Binding Message}"
                       TextWrapping="Wrap"
                       Foreground="#FFEEEEEE"
                       FontSize="13"
                       VerticalAlignment="Center"/>
            
            <!-- Close button -->
            <Button Grid.Column="2"
                    Content="✕"
                    FontSize="14"
                    Margin="12,0,0,0"
                    VerticalAlignment="Top"
                    Padding="6,2"
                    Cursor="Hand"
                    Click="CloseButton_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="#FFCCCCCC"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            Padding="{TemplateBinding Padding}"
                                            CornerRadius="3">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#33FFFFFF"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </Border>
</UserControl>
