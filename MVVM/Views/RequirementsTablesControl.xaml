<UserControl x:Class="TestCaseEditorApp.MVVM.Views.RequirementsTablesControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:helpers="clr-namespace:TestCaseEditorApp.Helpers"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="500">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </UserControl.Resources>

    <DockPanel LastChildFill="True" Margin="6">
        <!-- Top action row (visibility bound to ShowLocalToolbar DP on the UserControl) -->
        <StackPanel DockPanel.Dock="Top"
                    Orientation="Horizontal"
                    Margin="0,0,0,8"
                    Visibility="{Binding ShowLocalToolbar, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BoolToVisibilityConverter}}">
            <Button Content="Select All" Command="{Binding SelectAllTablesCommand}" Margin="0,0,8,0"/>
            <Button Content="Clear All" Command="{Binding ClearAllTablesCommand}"/>
        </StackPanel>

        <!-- Tables list -->
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding SelectedTableVMs}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="8" Margin="0,0,0,12" BorderBrush="#22000000" BorderThickness="1" CornerRadius="4" Background="White">
                            <StackPanel>
                                <Grid Margin="0,0,0,6">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,8,0">
                                        <CheckBox Margin="0,0,8,0"
                                                  Content="Include in LLM Prompt"
                                                  IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                        <TextBlock Text="{Binding Title}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                        <Button Content="Edit" Command="{Binding EditTableCommand}" Margin="8,0,0,0" Padding="8,2"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="Rows:" FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding Rows.Count}" Margin="6,0,0,0"/>
                                        <TextBlock Text="Cols:" FontWeight="SemiBold" Margin="12,0,0,0"/>
                                        <TextBlock Text="{Binding Columns.Count}" Margin="6,0,0,0"/>
                                    </StackPanel>
                                </Grid>

                                <DataGrid ItemsSource="{Binding Rows}"
                                          AutoGenerateColumns="False"
                                          HeadersVisibility="Column"
                                          CanUserAddRows="False"
                                          IsReadOnly="True"
                                          MinHeight="140"
                                          ScrollViewer.VerticalScrollBarVisibility="Auto"
                                          helpers:DataGridColumnsBinder.ColumnsSource="{Binding Columns}"
                                          EnableRowVirtualization="True"
                                          EnableColumnVirtualization="True"
                                          ScrollViewer.CanContentScroll="True"
                                          VirtualizingPanel.IsVirtualizing="True"
                                          VirtualizingPanel.VirtualizationMode="Recycling"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>

                <!-- Empty state -->
                <ItemsControl.Style>
                    <Style TargetType="ItemsControl">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectedTableVMs.Count}" Value="0">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ItemsControl">
                                            <Border Margin="8" Padding="12" BorderBrush="#22000000" BorderThickness="1" CornerRadius="6" Background="#FFFDFDFD">
                                                <TextBlock Opacity="0.75" Text="No support tables found for this requirement."/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ItemsControl.Style>
            </ItemsControl>
        </ScrollViewer>
    </DockPanel>
</UserControl>