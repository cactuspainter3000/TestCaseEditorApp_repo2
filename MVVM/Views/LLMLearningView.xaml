<UserControl x:Class="TestCaseEditorApp.MVVM.Views.LLMLearningView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/DesignSystem.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Background="{StaticResource Brush.Background.Menu}">
        <StackPanel VerticalAlignment="Center" 
                    HorizontalAlignment="Center" 
                    MaxWidth="600"
                    Margin="40">
            
            <!-- LLM Learning header -->
            <TextBlock Text="LLM Learning"
                       FontSize="28"
                       FontWeight="Light"
                       Foreground="{StaticResource Brush.Accent.Primary}"
                       TextAlignment="Center"
                       Margin="0,0,0,20"/>
            
            <!-- Description -->
            <TextBlock Text="Train and configure AI models for intelligent test case generation"
                       FontSize="16"
                       Foreground="#CCCCCC"
                       TextAlignment="Center"
                       TextWrapping="Wrap"
                       Margin="0,0,0,40"/>
            
            <!-- LLM Learning section -->
            <Border Background="#333333"
                    CornerRadius="8"
                    Padding="30"
                    Margin="0,20,0,0">
                <StackPanel>
                    <TextBlock Text="AI Model Training"
                               FontSize="18"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource Brush.Accent.Primary}"
                               Margin="0,0,0,20"/>
                    
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                        <TextBlock Text="•" Foreground="#CCCCCC" Margin="0,0,10,0" VerticalAlignment="Top"/>
                        <TextBlock Text="Configure LLM settings and model preferences"
                                   Foreground="#CCCCCC"
                                   TextWrapping="Wrap"/>
                    </StackPanel>
                    
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                        <TextBlock Text="•" Foreground="#CCCCCC" Margin="0,0,10,0" VerticalAlignment="Top"/>
                        <TextBlock Text="Train models on domain-specific requirements and patterns"
                                   Foreground="#CCCCCC"
                                   TextWrapping="Wrap"/>
                    </StackPanel>
                    
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                        <TextBlock Text="•" Foreground="#CCCCCC" Margin="0,0,10,0" VerticalAlignment="Top"/>
                        <TextBlock Text="Fine-tune AI responses for better test case quality"
                                   Foreground="#CCCCCC"
                                   TextWrapping="Wrap"/>
                    </StackPanel>
                    
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="•" Foreground="#CCCCCC" Margin="0,0,10,0" VerticalAlignment="Top"/>
                        <TextBlock Text="Monitor and evaluate model performance metrics"
                                   Foreground="#CCCCCC"
                                   TextWrapping="Wrap"/>
                    </StackPanel>
                </StackPanel>
            </Border>
            
        </StackPanel>
    </Grid>
</UserControl>