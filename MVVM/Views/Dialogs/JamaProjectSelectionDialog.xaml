<Window x:Class="TestCaseEditorApp.MVVM.Views.Dialogs.JamaProjectSelectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Jama Project"
        Height="400"
        Width="600"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Background="#2d2d30"
        Foreground="#f1f1f1">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#f1f1f1"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>
        
        <Style TargetType="ListBox">
            <Setter Property="Background" Value="#3c3c3c"/>
            <Setter Property="Foreground" Value="#f1f1f1"/>
            <Setter Property="BorderBrush" Value="#007acc"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        
        <Style TargetType="ListBoxItem">
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#f1f1f1"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#007acc"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#4d4d4d"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style TargetType="Button">
            <Setter Property="Background" Value="#007acc"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1f8ad4"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#505050"/>
                    <Setter Property="Cursor" Value="Arrow"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Text="📁 Select Target Project for Test Cases"
                   FontSize="18"
                   FontWeight="SemiBold"
                   Margin="0,0,0,10"
                   HorizontalAlignment="Center"/>

        <!-- Instructions -->
        <TextBlock Grid.Row="1" 
                   Text="Choose the Jama project where your test cases will be imported:"
                   FontSize="14"
                   Margin="0,0,0,15"
                   TextWrapping="Wrap"/>

        <!-- Project List -->
        <ListBox Grid.Row="2" 
                 x:Name="ProjectListBox"
                 SelectionMode="Single"
                 SelectionChanged="ProjectListBox_SelectionChanged">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Vertical" Margin="5">
                        <TextBlock FontWeight="SemiBold" FontSize="16">
                            <Run Text="🎯 "/>
                            <Run Text="{Binding ProjectKey}"/>
                        </TextBlock>
                        <TextBlock Text="{Binding Name}" 
                                   FontSize="14" 
                                   Opacity="0.9"
                                   Margin="0,2,0,0"/>
                        <TextBlock FontSize="12" 
                                   Opacity="0.7"
                                   Margin="0,2,0,0">
                            <Run Text="ID: "/>
                            <Run Text="{Binding Id}"/>
                            <Run Text=" • Created: "/>
                            <Run Text="{Binding CreatedDate}"/>
                        </TextBlock>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,15,0,0">
            <Button Content="Cancel" 
                    IsCancel="True" 
                    Click="Cancel_Click"
                    Background="#505050"
                    Margin="0,0,10,0"/>
            
            <Button x:Name="SelectButton"
                    Content="Import to Project" 
                    IsDefault="True" 
                    Click="Select_Click"
                    IsEnabled="False"/>
        </StackPanel>
    </Grid>
</Window>