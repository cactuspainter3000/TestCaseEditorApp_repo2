<UserControl x:Class="TestCaseEditorApp.MVVM.Domains.Startup.Views.StartupHeaderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:TestCaseEditorApp.MVVM.Domains.Startup.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="60" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=vm:StartupHeaderVM}">
    
    <Grid Margin="3" Background="Transparent">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <!-- Simple Header Text -->
        <Border Grid.Column="0"
                Background="{StaticResource Brush.Background.Card}"
                BorderBrush="{StaticResource Brush.Background.Surface}"
                BorderThickness="1"
                CornerRadius="4"
                Padding="12,8"
                Margin="0,3,8,0" 
                HorizontalAlignment="Stretch"
                VerticalAlignment="Center">
            <TextBlock Text="{Binding Text}"
                       Style="{StaticResource Text.Body}"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>
        </Border>
    </Grid>
</UserControl>