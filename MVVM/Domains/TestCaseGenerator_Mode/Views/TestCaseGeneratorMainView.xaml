<UserControl x:Class="TestCaseEditorApp.MVVM.Domains.TestCaseGenerator_Mode.Views.TestCaseGeneratorMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    
    <!-- Main workspace content -->
    <Grid>
            <Border Background="{StaticResource Brush.Background.Menu}" 
                    BorderBrush="{StaticResource CardBorderBrush}" 
                    BorderThickness="1" 
                    CornerRadius="6" 
                    Margin="20">
                
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="40">
                    
                    <!-- Icon - more subtle -->
                    <TextBlock Text="📋" 
                               FontSize="32" 
                               HorizontalAlignment="Center" 
                               Margin="0,0,0,16" 
                               Opacity="0.7"/>
                    
                    <!-- Title -->
                    <TextBlock Text="{Binding Title}" 
                               FontSize="24"
                               FontWeight="Bold"
                               HorizontalAlignment="Center" 
                               Margin="0,0,0,12"/>
                    
                    <!-- Description -->
                    <TextBlock Text="{Binding Description}" 
                               FontSize="14"
                               HorizontalAlignment="Center" 
                               TextAlignment="Center" 
                               TextWrapping="Wrap" 
                               MaxWidth="500" 
                               Opacity="0.8" 
                               Margin="0,0,0,24"/>
                    
                    <!-- Get Started Message -->
                    <TextBlock Text="Get started by selecting an option from the side menu" 
                               FontSize="12"
                               HorizontalAlignment="Center" 
                               Opacity="0.6"/>
                    
                </StackPanel>
            </Border>
    </Grid>
</UserControl>