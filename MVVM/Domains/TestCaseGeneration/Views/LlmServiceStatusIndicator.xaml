<UserControl x:Class="TestCaseEditorApp.MVVM.Domains.TestCaseGeneration.Views.LlmServiceStatusIndicator"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="300">
    <Grid Background="Transparent">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        
        <!-- Status Indicator Circle -->
        <Ellipse Grid.Column="0" 
                 Width="12" Height="12" 
                 Fill="{Binding StatusColor, RelativeSource={RelativeSource AncestorType=UserControl}}"
                 Margin="0,0,8,0"
                 VerticalAlignment="Center"/>
        
        <!-- Status Text and Service Info -->
        <StackPanel Grid.Column="1" Orientation="Vertical" VerticalAlignment="Center">
            <TextBlock Text="{Binding StatusText, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       FontWeight="Medium"
                       FontSize="12"
                       Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
            <TextBlock Text="{Binding ServiceInfo, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       FontSize="10"
                       Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                       Margin="0,1,0,0"/>
        </StackPanel>
        
        <!-- Fallback Warning Icon -->
        <Grid Grid.Column="2" 
              Visibility="{Binding FallbackIndicatorVisibility, RelativeSource={RelativeSource AncestorType=UserControl}}"
              Margin="8,0,0,0"
              VerticalAlignment="Center"
              ToolTip="LLM service unavailable - using fallback mode">
            <Polygon Points="0,10 5,0 10,10" 
                     Fill="Orange" 
                     Stroke="DarkOrange" 
                     StrokeThickness="1"
                     Width="12" Height="12"/>
            <TextBlock Text="!" 
                       FontSize="8" 
                       FontWeight="Bold" 
                       Foreground="White"
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center"
                       Margin="0,-1,0,0"/>
        </Grid>
    </Grid>
</UserControl>