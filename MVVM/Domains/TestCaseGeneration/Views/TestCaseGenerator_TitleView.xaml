<UserControl x:Class="TestCaseEditorApp.MVVM.Domains.TestCaseGeneration.Views.TestCaseGenerator_TitleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:conv="clr-namespace:TestCaseEditorApp.Converters"
             mc:Ignorable="d"
             d:DesignHeight="40" d:DesignWidth="400">
    
    <UserControl.Resources>
        <conv:NullToVisibilityConverter x:Key="NullToVisibilityConverter" />
    </UserControl.Resources>

    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
        <!-- App Title -->
        <TextBlock Text="Test Case Editor"
                   VerticalAlignment="Center"
                   Foreground="DarkOrange"
                   FontSize="25"
                   Margin="0,0,20,0"/>
        
        <!-- Save Indicator -->
        <Button Padding="8,4"
                Background="Transparent"
                BorderThickness="0"
                VerticalAlignment="Center"
                Command="{Binding SaveWorkspaceCommand}"
                ToolTip="{Binding SaveStatusText}"
                Visibility="{Binding WorkspaceFilePath, Converter={StaticResource NullToVisibilityConverter}}">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        Padding="{TemplateBinding Padding}"
                                        CornerRadius="3">
                                    <ContentPresenter />
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#15FFFFFF"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
            <StackPanel Orientation="Horizontal" Opacity="0.8">
                <TextBlock FontFamily="Segoe MDL2 Assets"
                           FontSize="14"
                           VerticalAlignment="Center"
                           Margin="0,0,6,0">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Text" Value="&#xE74E;"/> <!-- Save icon -->
                            <Setter Property="Foreground" Value="#4CAF50"/> <!-- Green = Saved -->
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsDirty}" Value="True">
                                    <Setter Property="Text" Value="&#xE734;"/> <!-- Warning icon -->
                                    <Setter Property="Foreground" Value="#FF9800"/> <!-- Orange = Unsaved -->
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
                <TextBlock Text="{Binding SaveStatusText}"
                           VerticalAlignment="Center"
                           FontSize="12"
                           Foreground="#666"/>
            </StackPanel>
        </Button>
    </StackPanel>
</UserControl>