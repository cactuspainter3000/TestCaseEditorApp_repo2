<UserControl x:Class="TestCaseEditorApp.MVVM.Domains.Dummy.Views.Dummy_MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="400"
             Background="#2B2B2B">
    
    <Border BorderBrush="#00FF00" BorderThickness="2" CornerRadius="8" Padding="20">
        <StackPanel>
            <TextBlock Text="MAIN WORKSPACE" 
                       FontSize="16" FontWeight="Bold" 
                       Foreground="#00FF00" 
                       HorizontalAlignment="Center" Margin="0,0,0,10"/>
            
            <TextBlock Text="{Binding DisplayText}" 
                       FontSize="14" FontWeight="Bold" 
                       Foreground="White" 
                       HorizontalAlignment="Center" Margin="0,0,0,10"/>
            
            <TextBlock Text="{Binding StatusMessage}" 
                       FontSize="12" 
                       Foreground="#CCCCCC" 
                       TextWrapping="Wrap"
                       HorizontalAlignment="Center" Margin="0,0,0,10"/>
            
            <TextBlock Text="{Binding SectionName, StringFormat='Section: {0}'}" 
                       FontSize="11" 
                       Foreground="#888888" 
                       HorizontalAlignment="Center" Margin="0,0,0,5"/>
            
            <TextBlock Text="{Binding LastUpdated, StringFormat='Updated: HH:mm:ss'}" 
                       FontSize="10" 
                       Foreground="#666666" 
                       HorizontalAlignment="Center"/>
        </StackPanel>
    </Border>
</UserControl>