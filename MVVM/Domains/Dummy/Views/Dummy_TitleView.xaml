<UserControl x:Class="TestCaseEditorApp.MVVM.Domains.Dummy.Views.Dummy_TitleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="400"
             Background="#1A1A1A">
    
    <Border BorderBrush="#FF69B4" BorderThickness="2" CornerRadius="5" Padding="12">
        <StackPanel>
            <TextBlock Text="TITLE WORKSPACE" 
                       FontSize="10" FontWeight="Bold" 
                       Foreground="#FF69B4" 
                       HorizontalAlignment="Center" Margin="0,0,0,4"/>
            
            <TextBlock Text="{Binding PageTitle}" 
                       FontSize="16" FontWeight="Bold" 
                       Foreground="White" 
                       HorizontalAlignment="Center" Margin="0,0,0,2"/>
            
            <DockPanel>
                <TextBlock Text="{Binding Breadcrumb}" 
                           FontSize="10" 
                           Foreground="#CCCCCC" 
                           DockPanel.Dock="Left"/>
                
                <TextBlock Text="{Binding LastUpdated, StringFormat='(HH:mm:ss)'}" 
                           FontSize="8" 
                           Foreground="#666666" 
                           HorizontalAlignment="Right"/>
            </DockPanel>
            
            <!-- Interactive Demo -->
            <Button Content="Title" 
                    Command="{Binding TestButtonCommand}"
                    VerticalAlignment="Center"
                    MinWidth="40"
                    Padding="6,2"
                    FontSize="9">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#4A4A4A"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="BorderBrush" Value="#FF69B4"/>
                        <Setter Property="BorderThickness" Value="1"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="3">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#FF69B4"/>
                                            <Setter Property="BorderBrush" Value="#FF8CC8"/>
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter Property="Background" Value="#E055A3"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
                    
            <TextBlock Text="{Binding SharedMessage}" 
                       FontSize="9" 
                       Foreground="#DDDDDD" 
                       HorizontalAlignment="Center" 
                       TextWrapping="Wrap"/>
        </StackPanel>
    </Border>
</UserControl>