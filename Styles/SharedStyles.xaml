<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib">

    <!-- Color tokens -->
    <Color x:Key="PrimaryColor">#FF2D7AFC</Color>
    <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}"/>
    <SolidColorBrush x:Key="SurfaceBrush" Color="#FFF7F7F7"/>
    <SolidColorBrush x:Key="CardBrush" Color="White"/>
    <SolidColorBrush x:Key="AccentBrush" Color="#FF2D7AFC"/>
    <SolidColorBrush x:Key="MutedTextBrush" Color="#FF666666"/>

    <!-- Basic styles -->
    <Style TargetType="Button" x:Key="PrimaryButton">
        <Setter Property="Padding" Value="8,6"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Margin" Value="4,0,0,0"/>
    </Style>

    <Style TargetType="Border" x:Key="Card">
        <Setter Property="Background" Value="{StaticResource CardBrush}"/>
        <Setter Property="CornerRadius" Value="6"/>
        <Setter Property="Padding" Value="8"/>
        <Setter Property="Margin" Value="4"/>
    </Style>

    <!-- Left menu item -->
    <DataTemplate x:Key="LeftMenuItemTemplate">
        <DockPanel Margin="6">
            <TextBlock Text="{Binding Title}" VerticalAlignment="Center" Foreground="{StaticResource MutedTextBrush}"/>
            <TextBlock DockPanel.Dock="Right" Text="{Binding Badge}" Foreground="{StaticResource PrimaryBrush}" Margin="6,0,0,0"/>
        </DockPanel>
    </DataTemplate>

</ResourceDictionary>