<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:conv="clr-namespace:TestCaseEditorApp.Converters">

    <!-- Reuse/create theme brushes for menus so popup uses same look as the rest of the app -->
    <SolidColorBrush x:Key="MenuListBackground" Color="#252525"/>
    <SolidColorBrush x:Key="MenuListForeground" Color="White"/>

    <!-- Converter for badge visibility -->
    <conv:BadgeToVisibilityConverter x:Key="BadgeToVisibilityConverter"/>

    <!-- Button controls used in the window chrome -->
    <Style x:Key="MainWindowButtonControls" TargetType="Button">
        <Setter Property="Width" Value="28"/>
        <Setter Property="Height" Value="22"/>
        <Setter Property="Margin" Value="2"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="FontSize" Value="12"/>
    </Style>

    <!-- Menu header text style -->
    <Style x:Key="MenuHeaderTheme" TargetType="TextBlock">
        <Setter Property="Foreground" Value="DarkOrange"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Margin" Value="6,4,6,4"/>
    </Style>

    <Style x:Key="MenuListBoxItemStyle" TargetType="ListBoxItem">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <!-- remove focus rectangle -->
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border x:Name="Bd" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                        <ContentPresenter
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                            Content="{TemplateBinding Content}"
                            ContentTemplate="{TemplateBinding ContentTemplate}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#333333"/>
                            <!-- subtle dark selection -->
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#2A2A2A"/>
                            <!-- hover -->
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Menu visuals: remove icy-blue focus and use theme-friendly colors -->

<!-- Header Toggle (File) style: no focus glow, hover/checked backgrounds tuned) -->
    <Style x:Key="MenuHeaderToggleStyle" TargetType="ToggleButton">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <!-- removes icy-blue focus rectangle -->
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border x:Name="Root" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="#2A2A2A"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="#333333"/>
                            <Setter Property="Foreground" Value="DarkOrange"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Popup menu button style (readable, no icy focus) -->
    <Style x:Key="MenuPopupButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="MinWidth" Value="220"/>
        <Setter Property="MinHeight" Value="28"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <!-- removes icy-blue focus rectangle -->
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="2" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#2A2A2A"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#3A3A3A"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- RadioButton menu items (StepDescriptorTemplate) - disable default focus glow and tune checked/hover -->
    <Style x:Key="MenuButtonTheme" TargetType="RadioButton">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Padding" Value="6"/>
        <Setter Property="Margin" Value="2,2,2,2"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <!-- remove default focus indication -->
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="6"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Rectangle x:Name="AccentBar" Grid.Column="0" Fill="Transparent" RadiusX="2" RadiusY="2"/>
                        <Border x:Name="MainBorder" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" Grid.Column="1">
                            <ContentPresenter VerticalAlignment="Center" Margin="6,2"/>
                        </Border>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="AccentBar" Property="Fill" Value="DarkOrange"/>
                            <Setter TargetName="MainBorder" Property="Background" Value="#333333"/>
                            <Setter Property="Foreground" Value="Orange"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="MainBorder" Property="Background" Value="#2A2A2A"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- DataTemplate used by original XAML -->
    <DataTemplate x:Key="CommandsTemplate">
        <TextBlock Text="{Binding}" Foreground="White"/>
    </DataTemplate>

    <!-- StepDescriptorTemplate uses the converter to hide zero or empty badges -->
    <DataTemplate x:Key="StepDescriptorTemplate">
        <RadioButton Style="{StaticResource MenuButtonTheme}"
               IsChecked="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=IsSelected, Mode=TwoWay}"
               Margin="0,2,0,2">
            <DockPanel LastChildFill="True">
                <TextBlock Text="{Binding DisplayName}" Foreground="White" VerticalAlignment="Center" DockPanel.Dock="Left"/>

                <!-- Badge chip on the right -->
                <Border Background="DarkOrange"
              CornerRadius="10"
              Padding="6,2"
              Margin="8,0,0,0"
              VerticalAlignment="Center"
              DockPanel.Dock="Right"
              Visibility="{Binding Badge, Converter={StaticResource BadgeToVisibilityConverter}}">
                    <TextBlock Text="{Binding Badge}" Foreground="White" FontSize="12" VerticalAlignment="Center"/>
                </Border>
            </DockPanel>
        </RadioButton>
    </DataTemplate>

</ResourceDictionary>