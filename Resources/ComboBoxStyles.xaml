<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:conv="clr-namespace:TestCaseEditorApp.Converters">

    <!-- ========================================== -->
    <!-- COMBOBOX STYLES                           -->
    <!-- ========================================== -->
    <!-- All ComboBox styles reference DesignSystem.xaml tokens. -->

    <!-- Merge DesignSystem for access to tokens -->
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="DesignSystem.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <!-- converter -->
    <conv:BoolToAngleConverter x:Key="BoolToAngleConverter" />

    <Style x:Key="CardComboBoxStyle" TargetType="ComboBox" BasedOn="{StaticResource MenuPopupComboBoxStyle}">
        <!-- default collapsed text color -->
        <Setter Property="Foreground" Value="{StaticResource Brush.Text.Primary}" />
        <Setter Property="Background" Value="{StaticResource Brush.Background.Menu}" />
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Border.Subtle}" />
        <Setter Property="Padding" Value="6,2" />

        <!-- ensure panel uses the grey background so whole dropdown is grey -->
        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel Background="{StaticResource Brush.Background.Menu}" />
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <Border x:Name="Bd"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="4"
                    SnapsToDevicePixels="True">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="38" />
                                </Grid.ColumnDefinitions>

                                <!-- Selected content / collapsed view -->
                                <ContentPresenter x:Name="ContentSite"
                                                  Grid.Column="0"
                                                  Margin="6,0"
                                                  VerticalAlignment="Center"
                                                  HorizontalAlignment="Left"
                                                  Content="{TemplateBinding SelectionBoxItem}"
                                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                  ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                  RecognizesAccessKey="True"
                                                  TextElement.Foreground="{TemplateBinding Foreground}" />

                                <!-- ToggleButton (arrow) -->
                                <ToggleButton x:Name="ToggleButton"
                                              Grid.Column="1"
                                              Width="38"
                                              HorizontalAlignment="Right"
                                              Focusable="False"
                                              IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                              ClickMode="Press"
                                              Background="{TemplateBinding Background}"
                                              BorderThickness="0"
                                              Padding="6,0"
                                              HorizontalContentAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              FocusVisualStyle="{x:Null}">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="ToggleButton">
                                            <Border x:Name="ArrowBd"
                                                                    Background="{TemplateBinding Background}"
                                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                                      BorderThickness="0"
                                                                      CornerRadius="2"
                                                                      Padding="{TemplateBinding Padding}">
                                                <Grid Width="18" Height="12" HorizontalAlignment="Center" VerticalAlignment="Center">
                                                    <Path x:Name="Chevron"
                                                                        Data="M1,4 L9,10 L17,4"
                                                                        Stroke="{StaticResource Brush.Accent.Primary}"
                                                                        Fill="Transparent"
                                                                        StrokeThickness="1.8"
                                                                        StrokeStartLineCap="Round"
                                                                        StrokeEndLineCap="Round"
                                                                        StrokeLineJoin="Round"
                                                                        RenderTransformOrigin="0.5,0.5">
                                                        <Path.RenderTransform>
                                                            <RotateTransform Angle="{Binding IsChecked, RelativeSource={RelativeSource AncestorType=ToggleButton}, Converter={StaticResource BoolToAngleConverter}}" />
                                                        </Path.RenderTransform>
                                                    </Path>
                                                </Grid>
                                            </Border>

                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="ArrowBd" Property="Background" Value="{StaticResource Brush.Background.Overlay.Light}" />
                                                    <Setter TargetName="Chevron" Property="Stroke" Value="{StaticResource Brush.Accent.Primary.Hover}" />
                                                </Trigger>
                                                <Trigger Property="IsPressed" Value="True">
                                                    <Setter TargetName="ArrowBd" Property="Background" Value="{StaticResource Brush.Background.Overlay.Medium}" />
                                                    <Setter TargetName="Chevron" Property="Stroke" Value="{StaticResource Brush.Accent.Primary.Hover}" />
                                                </Trigger>
                                                <Trigger Property="IsEnabled" Value="False">
                                                    <Setter Property="Opacity" Value="0.5"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                </ToggleButton>
                            </Grid>
                        </Border>

                        <!-- Popup / dropdown surface -->
                        <Popup x:Name="Popup"
                   Placement="Bottom"
                   AllowsTransparency="True"
                   IsOpen="{TemplateBinding IsDropDownOpen}"
                   Focusable="False"
                   PopupAnimation="Fade">
                            <Border Background="{StaticResource Brush.Background.Menu}"
                      BorderBrush="{StaticResource Brush.Background.Menu}"
                      BorderThickness="1"
                      CornerRadius="{StaticResource Radius.MD}"
                      SnapsToDevicePixels="True"
                      Padding="2"
                      MinWidth="{Binding ActualWidth, ElementName=Bd}">
                                <ScrollViewer Background="{StaticResource Brush.Background.Card}"
                              CanContentScroll="True" MaxHeight="300">
                                    <ScrollViewer.Resources>
                                        <Style TargetType="ScrollBar" BasedOn="{StaticResource CustomScrollBarStyle}"/>
                                    </ScrollViewer.Resources>
                                    <Border Background="{StaticResource Brush.Background.Menu}" SnapsToDevicePixels="True" Padding="0">
                                        <ItemsPresenter />
                                    </Border>
                                </ScrollViewer>
                            </Border>
                        </Popup>

                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="HasItems" Value="false">
                            <Setter TargetName="Popup" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{StaticResource Brush.Text.Tertiary}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <!-- Dropdown item visuals -->
        <Setter Property="ItemContainerStyle">
            <Setter.Value>
                <Style TargetType="ComboBoxItem">
                    <!-- default text in list -->
                    <Setter Property="Foreground" Value="{StaticResource Brush.Text.Primary}" />
                    <!-- items transparent so panel grey shows through -->
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="Padding" Value="6,4"/>
                    <Setter Property="HorizontalContentAlignment" Value="Left"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ComboBoxItem">
                                <Border x:Name="ItemBd"
                        Background="{TemplateBinding Background}"
                        Padding="{TemplateBinding Padding}"
                        SnapsToDevicePixels="True">
                                    <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                      VerticalAlignment="Center"
                                                      Content="{TemplateBinding Content}"
                                                      ContentTemplate="{TemplateBinding ContentTemplate}"
                                                      RecognizesAccessKey="True"
                                                      TextElement.Foreground="{TemplateBinding Foreground}" />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <!-- On mouseover: item background overlay + ORANGE text -->
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="ItemBd" Property="Background" Value="#20333333" />
                                        <Setter Property="Foreground" Value="{StaticResource Brush.Accent.Primary}" />
                                    </Trigger>
                                    <!-- When selected: AccentBrush background and WHITE text for legibility -->
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="ItemBd" Property="Background" Value="{StaticResource Brush.Accent.Primary}" />
                                        <Setter Property="Foreground" Value="{StaticResource Brush.Text.OnAccent}" />
                                    </Trigger>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter Property="Opacity" Value="0.5" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Setter.Value>
        </Setter>

    </Style>
</ResourceDictionary>